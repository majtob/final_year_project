# Dependencies for the Streamlit demo (Docker / examiner image).
# Includes xgboost + shap for the multisource model and waterfalls; no PyTorch / transformers.

streamlit>=1.28.0
pandas>=2.1.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
xgboost>=2.0.0
matplotlib>=3.8.0
shap>=0.43.0
pyarrow>=14.0.0
