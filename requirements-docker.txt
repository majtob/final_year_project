# Minimal dependencies for the Streamlit demo (Docker / examiner image).
# Keeps the image small — no PyTorch, transformers, or scraping stack.

streamlit>=1.28.0
pandas>=2.1.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
matplotlib>=3.8.0
shap>=0.43.0
pyarrow>=14.0.0
