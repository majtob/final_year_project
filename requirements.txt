# Data Collection & Web Scraping
requests>=2.31.0
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
selenium>=4.15.0
webdriver-manager>=4.0.0

# PDF Processing & OCR
pdfplumber>=0.10.0
pymupdf>=1.23.0
pytesseract>=0.3.10

# Data Processing
# Exact pins below: these versions produced the committed results/*.json and
# figures/*.png. A minor bump reorders the 21-feature benchmark on 45 rows even
# with random_state=42 throughout (see PROJECT_REPORT.md 15.5.1), so
# reproducing the artifacts needs the versions, not just the floors.
pandas==3.0.6
numpy==2.5.3
pyarrow>=14.0.0

# Machine Learning
scikit-learn==1.9.1
xgboost==3.4.1
shap==0.52.0

# Database
duckdb>=0.9.0

# Data Validation
pandera>=0.17.0
pydantic>=2.5.0

# Text Processing & NLP
langdetect>=1.0.9
ftfy>=6.1.1
unidecode>=1.3.7

# Market Data
yfinance>=0.2.28

# LLM Integration (open-source)
transformers>=4.35.0
torch>=2.1.0
safetensors>=0.4.0
accelerate>=0.25.0
ollama>=0.1.0
huggingface_hub>=0.19.0

# Sentence Embeddings (optional, for Option 2)
sentence-transformers>=2.2.2

# Logging
loguru>=0.7.2

# Configuration Management
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Jupyter & Notebooks
jupyter>=1.0.0
ipykernel>=6.26.0

# Utilities
tqdm>=4.66.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Demo app
streamlit>=1.28.0

# Statistics
# Pinned with the ML stack above: scikit-learn's lbfgs / liblinear solvers run
# through scipy, so it affects the linear models in the 21-feature table.
scipy==1.18.1
